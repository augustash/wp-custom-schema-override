# Wordpress Custom Schema Override Plugin

- Provides a custom JSON-LD field in the page meta sidebar that overrides schema structured data output when populated.
- Requires the use of the Yoast SEO plugin